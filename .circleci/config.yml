version: 2.1

orbs:
    thunderscan: defensecode/thunderscan@dev:69a5fa5

workflows:
  build:
    jobs:
      - thunderscan/scan:
          engines: "2"
          report: true
          report-filename: thunderscan-test-report
          report-format: html,json
          scan-name: "$CIRCLE_PROJECT_REPONAME/$CIRCLE_BUILD_NUM"


